# Admin dashboard implementation progress

## Iteration 1 — invite-secret foundation

| Field | Status |
| --- | --- |
| Dependency | `invite-security-remediation` must precede every admin-dashboard delivery and the P0 opaque-token/backend-redemption requirement. |
| Status | **Complete for this bounded foundation only.** P0 invite remediation is not yet complete. |
| Scope | Cryptographically random opaque token generation, keyed HMAC lookup representation, atomic-reservation collision retry, and raw-token redaction/non-persistence boundary. No invite lifetime, use, retention, identity, privacy, billing, moderation, or legal policy was added. |
| Files | `functions/src/invites/inviteSecrets.ts`; `functions/test/unit/inviteSecrets.test.ts`; this progress record. |
| Focused verification evidence | TDD first: `npm test -- --run test/unit/inviteSecrets.test.ts` failed because `src/invites/inviteSecrets.js` did not exist. After implementation, `npx vitest run test/unit/inviteSecrets.test.ts` passed: 1 file / 6 tests. `npm run build` passed. `npx biome check src/invites/inviteSecrets.ts test/unit/inviteSecrets.test.ts` passed after formatting. |
| Security evidence | Tokens are 32 CSPRNG bytes (256 bits) encoded as opaque base64url; legacy `KS-` values fail opaque-token validation; persistent representation is versioned HMAC-SHA-256 only; lookup reservation receives no raw token; collision attempts retry and exhausted failures use a generic error; JSON/string/diagnostic rendering of the raw-token wrapper is redacted. |
| Blockers | The existing client-derived invite issue/lookup/redeem flow, Firestore Rules, and migration remain unchanged. A trusted backend issuance/redemption implementation still needs deployment-provided server-only HMAC-key wiring and an atomic Firestore reservation/write design. Those prerequisites must be completed without placing a key or raw token in source, records, logs, analytics, or callable input. |
| Next dependency-safe work | Add the trusted backend issuance path that authorizes the household Admin and atomically reserves/persists only opaque invite metadata plus this HMAC lookup representation. Keep expiry, use-limit, revocation, retention, and identity policy out of scope until separately decided. |

## Iteration 2 — trusted household-Admin invite issuance

| Field | Status |
| --- | --- |
| Dependency | Builds on Iteration 1's opaque-token/HMAC foundation. It does not make legacy client invites safe or release any client path. |
| Status | **Complete for this bounded issuance unit only.** P0 invite remediation remains incomplete until backend redemption, Rules lockdown, legacy invalidation/migration, and the separately decided lifecycle controls are delivered. |
| Scope | Added the `issueHouseholdInvite` callable. It uses the repository's callable App Check configuration and non-anonymous caller boundary, requires a current household `admin`, and preserves the existing joint-household/current-Premium eligibility gate. Its strict schema accepts only `householdId`, `role` (`member`, `shopper`, or `cook`), and a path-safe `commandId`. |
| Server secret configuration | `INVITE_TOKEN_HMAC_KEY` is a Firebase Functions `defineSecret` parameter bound only to `issueHouseholdInvite`; it is never read from callable data or ordinary source configuration. Before deployment, an operator must set it in Firebase Secret Manager to the canonical base64url encoding of at least 32 CSPRNG bytes (for example, through the Firebase CLI `functions:secrets:set` workflow). No value belongs in this repository, client configuration, logs, records, or test fixtures used as deployment configuration. |
| Atomic persistence | Every candidate token is HMAC-derived through `inviteSecrets` and is transactionally checked and written at `householdInviteTokens/{tokenLookupHmac}`. The record contains only `tokenLookupHmac`, HMAC version, opaque-format version, household ID, allowed role, issuer UID, and server issue time. The document ID is itself the permitted keyed lookup representation. The same transaction creates a non-secret `householdInviteIssueReceipts/{commandId}` receipt. No raw token, `active` flag, expiry, use count, revocation, retention, recipient, or redemption state is persisted. |
| One-time disclosure and idempotency | A fresh command returns `requestId`, metadata, `alreadyIssued: false`, and the raw token once. A matching command-ID replay requires current Admin authorization, returns `alreadyIssued: true` with the request ID and metadata, and intentionally omits the token; changed command data is denied. This is the only compatible reuse of the existing receipt pattern because replaying a stored token would violate the raw-token disclosure boundary. |
| Abuse controls deferred by policy | No reusable Functions rate-limit primitive exists in this repository, so no arbitrary account/IP bucket or limit was introduced. Issuance uses existing App Check/authentication controls. Per-account/IP limits and their storage/retention policy must be decided before the endpoint is consumer-released. |
| Focused verification evidence | TDD red: `npx vitest run test/unit/inviteIssuance.test.ts` first failed because `inviteIssuance.js` did not exist; the receipt test then failed while `commandId` was rejected by the initial schema. Green: the focused suite passed 7 tests, covering strict schema/authentication, Admin authorization, transaction-backed HMAC persistence/no raw token, collision retry, command replay/redaction/payload mismatch, runtime secret validation, and safe runtime-secret failure handling. |
| Files | `functions/src/invites/inviteIssuance.ts`; `functions/src/index.ts`; `functions/test/unit/inviteIssuance.test.ts`; this progress record. |
| Blockers | Existing client-derived `KS-` issuance, lookup, and redemption remain live; production/development Firestore Rules still permit legacy invite lookup and invite-based member creation; no legacy-invite invalidation/migration exists. Expiry, maximum active invites, one-time or multi-use semantics, revocation, recipient binding, capacity at redemption, rate limiting, and retention require an approved policy before redemption or consumer rollout. |
| Next dependency-safe work | Define the minimum invite lifecycle policy required for safe redemption (at least expiry, one-time/use limit, revocation, capacity/entitlement, rate-limit scope, and receipt retention), then implement trusted backend redemption and transaction tests. Only after that can Rules disable all legacy direct invite lookup/redemption/member creation and a migration invalidate existing `KS-` values. |

## Iteration 3 — backend-only one-time redemption and selected lifecycle

| Field | Status |
| --- | --- |
| Policy applied | Invites expire seven days after issuance; an otherwise eligible authenticated account may redeem once without an email binding; individual revocation is represented in lifecycle state; issuance does not revoke other invites; terminal invite/command metadata carries 90-day cleanup eligibility. The selected issuance/redeem rate limits and 30-day rate-record retention are deliberately not implemented in this unit. |
| Status | **Complete for the bounded backend redemption unit only.** The predictable-invite vulnerability remains live until rate limits, Rules lockdown, legacy invalidation/migration, and client migration are complete. |
| Issuance metadata | Issuance now persists trusted `issuedAt`, `expiresAt`, `status: "active"`, `redemptionLimit: 1`, `redemptionCount: 0`, explicit nullable redeemed/revoked fields, and `terminalCleanupEligibleAt` (expiry plus 90 days). Issuance receipts also carry a 90-day `cleanupEligibleAt`. Raw tokens remain absent. |
| Redemption command | Added App Check-protected `redeemHouseholdInvite`, bound to the same runtime-only `INVITE_TOKEN_HMAC_KEY` Secret Manager parameter. Its strict request has only `inviteToken` and path-safe `commandId`; it computes the HMAC lookup before reading the opaque token document. Legacy `KS-`, malformed, unknown, expired, revoked, used, topology, entitlement, capacity, duplicate-membership, and account-eligibility failures all return the same safe `failed-precondition` message plus server request ID. |
| Transaction invariants | A fresh redemption atomically validates active one-time lifecycle state, joint/current-Premium household state, valid `memberCount < maxMembers`, absent membership, and the existing free-account one-current-Premium-household rule (including expired trials). It creates the member with only role/timestamps, updates count/context, consumes the invite with the redeemer and a new 90-day terminal cleanup eligibility, and writes a non-secret `householdInviteRedemptionReceipts/{commandId}` receipt. An Auth identity without a profile receives the repository's minimal profile/context shape; existing profiles retain non-context fields. |
| Idempotency and secrecy | An exact redemption receipt is bound to the caller and HMAC lookup and returns `alreadyApplied: true`; a changed command/token or a later command against the consumed invite is generically denied. No member, user, invite, receipt, response, error, or logging path persists the raw bearer token. |
| Focused verification evidence | TDD red: `npx vitest run test/unit/inviteRedemption.test.ts` first failed because the redemption module did not exist; an added expired-trial regression then failed because the initial account check trusted stale `isPremium: true`. Green: final focused suites passed 4 files / 26 tests. The redemption suite has 10 tests covering strict/auth controls, legacy denial, one-time state transition and context updates, redacted replay, expired/revoked/used rejection, duplicate/free-account/expired-trial denial, topology/household-entitlement denial, and a deterministic optimistic-transaction capacity race where only one of two distinct invites commits. |
| Files | `functions/src/invites/inviteLifecycle.ts`; `functions/src/invites/inviteIssuance.ts`; `functions/src/invites/inviteRedemption.ts`; `functions/src/index.ts`; `functions/test/unit/inviteIssuance.test.ts`; `functions/test/unit/inviteRedemption.test.ts`; this progress record. |
| Blockers | Individual backend revocation command, rate-limit persistence/enforcement, terminal cleanup worker, Rules lockdown, legacy invite invalidation/migration, client callable migration, and end-to-end emulator/rules adversarial verification remain. Existing `householdInvites/{KS-*}` is still client-readable/redeemable and cannot be considered remediated yet. |
| Next dependency-safe work | Implement the selected issuance/redeem rate-limit stores and transaction-safe enforcement: 10 account/hour and 25 household/day for issuance; 20 account/hour and 60 IP/hour for redemption; retain rate records 30 days. Keep those records server-only, use safe generic denials, and add focused boundary/retry tests. |

## Iteration 4 — server-only transaction-safe invite rate limits

| Field | Status |
| --- | --- |
| Policy applied | Fixed UTC windows were selected from the explicit policy: issuance is 10 authenticated-account/fixed-hour and 25 household/fixed-day; redemption is 20 authenticated-account/fixed-hour and 60 source-IP/fixed-hour. Bucket records are cleanup-eligible 30 days after their window ends. |
| Status | **Complete for this bounded rate-limit unit only.** P0 invite remediation remains incomplete while legacy Rules/client paths can still redeem predictable `KS-` values. |
| Server secret configuration | Added independently bound `INVITE_RATE_LIMIT_KEY` through Firebase Functions `defineSecret`; it must be a canonical base64url encoding of at least 32 CSPRNG bytes, set through the Firebase Secret Manager workflow before deployment. Its value is never source-controlled, sent by a client, logged, or persisted. Both issue and redeem callables bind this secret in addition to `INVITE_TOKEN_HMAC_KEY`. |
| Privacy-preserving records | Counter documents are in server-only `inviteRateLimitBuckets/{rate-limit-hmac}`. Their deterministic IDs are domain-separated HMAC-SHA-256 values over operation, scope, fixed-window start, and subject. Records retain only HMAC bucket ID, operation/scope, count/limit, window timestamps, and cleanup eligibility—never raw account ID, household ID, IP address, token, request payload, or forwarded header. Redemption obtains an IP only from the Functions raw request socket `remoteAddress`; it deliberately ignores caller payload and `X-Forwarded-For` style headers. Missing socket metadata maps to a private HMAC sentinel bucket rather than bypassing the IP control. |
| Atomicity and replay ordering | All bucket reads occur before bucket writes and are committed in the same transaction as a successful invite issue/redeem. Authorization/state reads occur first; rate counters reserve before every customer-state write. Exact idempotency receipt replays are intentionally rate-charged, then return replay results only after the counter transaction commits. Invalid raw/legacy redemption attempts reserve account/IP buckets in a dedicated server transaction before their generic denial; valid opaque attempts, including rejected lifecycle/capacity/account states, commit their rate reservation before the generic denial. Collision-discarded generated issuance candidates do not consume quota. |
| Error contract | Exhausted buckets return callable `resource-exhausted` (the callable equivalent of HTTP 429) with only safe message, server request ID, and `retryAfterSeconds`; no target household/token/IP existence information is exposed. |
| Focused verification evidence | TDD red: `npx vitest run test/unit/inviteRateLimit.test.ts` first failed because the module did not exist; once added, the exact-boundary test caught a full-window (3600-second) retry value instead of the correct remaining-window (2700-second) value. Green: final focused suites passed 5 files / 34 tests. Rate tests cover all four exact limits, fixed-window retry, independent/domain-separated buckets, concurrent final-slot claims, 30-day cleanup eligibility, no raw identifier/IP persistence, raw-socket-only IP extraction, and runtime secret validation. Callable tests prove matching replays are charged and rate exhaustion occurs before invite/membership/customer-context writes. |
| Files | `functions/src/invites/inviteRateLimit.ts`; `functions/src/invites/inviteIssuance.ts`; `functions/src/invites/inviteRedemption.ts`; `functions/src/index.ts`; `functions/test/unit/inviteRateLimit.test.ts`; `functions/test/unit/inviteIssuance.test.ts`; `functions/test/unit/inviteRedemption.test.ts`; this progress record. |
| Blockers | Individual Admin revoke command, terminal cleanup worker, production Secret Manager provisioning, legacy-invite invalidation/migration, consumer callable migration, Firestore Rules lockdown, and emulator/Rules adversarial cutover testing remain. Existing `householdInvites/{KS-*}` is still client-readable and redeemable. |
| Next dependency-safe work | Implement a trusted household-Admin individual invite revocation command: strict HMAC lookup input, Admin authorization, active-to-revoked transaction state transition, 90-day terminal cleanup eligibility, safe idempotent receipt behavior, and focused adversarial tests. Only after all opaque issue/redeem/revoke controls are verified can Rules disable legacy direct invite access and membership creation. |

## Iteration 5 — trusted household-Admin individual invite revocation

| Field | Status |
| --- | --- |
| Policy applied | Seven-day, one-time invite lifecycle; individual household-Admin revocation only; issuance continues to leave other active invites intact; revoked invite and revocation-receipt metadata is eligible for cleanup 90 days after the trusted revocation time. |
| Status | **Complete for this bounded revocation unit only.** P0 predictable-invite remediation remains incomplete while legacy client/Rules flows can redeem `KS-` values. |
| Safe management identifier | Issuance now generates a separate CSPRNG 128-bit base64url `inviteId` and returns it as non-secret metadata on both fresh issuance and an authorized exact issuance replay. The primary invite remains keyed by server-only token HMAC; a server-only `householdInviteManagement/{inviteId}` index maps the opaque selector to that HMAC. The response never returns the HMAC. The identifier is only a selector, not a bearer-management credential: every revoke command reauthorizes current household Admin membership and current household invariants. |
| Revocation command | Added App Check-protected, non-anonymous `revokeHouseholdInvite` callable. Its strict request schema accepts only a 22-character opaque `inviteId` and path-safe `commandId`; it creates a request ID before work. In one transaction it resolves the management index, verifies the current owning-household Admin, current `isJoint` state, and current Premium entitlement, then permits only a structurally valid, unexpired, active one-time invite to become `revoked`. The primary invite records `revokedByUserId`, `revokedAt`, and `terminalCleanupEligibleAt`; the management record and a non-secret `householdInviteRevocationReceipts/{commandId}` receipt receive matching retention eligibility. |
| Idempotency and safe failures | Exact receipt replay is reauthorized against the current household state and returns only `requestId`, the caller-provided safe `inviteId`, and `alreadyRevoked: true`. Changed command reuse, non-Admin/foreign callers, unknown selectors, expired/redeemed/already-revoked invites, corrupted state, and failed current invariants return the same generic `failed-precondition` result without token, HMAC, or existence/state disclosure. |
| Abuse controls / policy gap | No revoke-specific rate limit was selected, so this unit deliberately did not invent a bucket or limit and does not repurpose issuance/redeem quotas. The callable retains existing App Check, authenticated non-anonymous caller, authoritative Admin/invariant, strict-schema, transactional, and idempotency controls. A revoke rate-limit policy remains a release-policy gap if product/security determines these controls are insufficient. |
| Focused verification evidence | TDD red: `npx vitest run test/unit/inviteRevocation.test.ts` initially failed because the revocation module did not exist. Green: focused invite/callable suites passed 6 files / 43 tests; the new revocation suite has 9 adversarial cases covering unauthenticated/non-strict input, non-Admin and foreign Admin denial, expired/redeemed/already-revoked denial, current topology/entitlement rechecks, 90-day retention, exact replay, command mismatch, and raw-token/HMAC response absence. Focused Biome check and `npm run build` passed. |
| Files | `functions/src/invites/inviteRevocation.ts`; `functions/src/invites/inviteIssuance.ts`; `functions/src/index.ts`; `functions/test/unit/inviteRevocation.test.ts`; `functions/test/unit/inviteIssuance.test.ts`; this progress record. |
| Blockers | A terminal cleanup worker; production Secret Manager provisioning; legacy-invite invalidation/migration; consumer callable migration; Firestore Rules lockdown; and emulator/Rules adversarial cutover verification remain. Existing `householdInvites/{KS-*}` remains client-readable/redeemable. |
| Next dependency-safe work | Implement and test a trusted bounded terminal-invite cleanup worker that deletes only opaque expired/redeemed/revoked invite metadata, management indexes, and command receipts after their recorded cleanup eligibility times. It must preserve active records, never process legacy invite records, use bounded batches/idempotent retries, and avoid raw-token data. |

## Iteration 6 — legacy invite Rules cutover

| Field | Status |
| --- | --- |
| Dependency | Follows the trusted opaque issue, redeem, rate-limit, and revocation callable implementations from Iterations 1–5. This is a Rules boundary only; it does not deploy Functions, provision production secrets, migrate Flutter, or delete legacy data. |
| Status | **Complete for the bounded Rules cutover unit.** Direct client access to predictable legacy `KS-*` invites and client-side invite redemption/member creation is denied by both checked-in Rules profiles. P0 remediation is not releasable until the client reaches the trusted callable flow and production deployment prerequisites are complete. |
| Rules lockdown | Removed the legacy `inviteDoc`, `isValidInviteJoin`, and `isValidJoinCapacityUpdate` paths. `householdInvites` now has an explicit unconditional deny, household `memberCount` can no longer be incremented through the direct invite batch, and only independently reserved initial-Admin provisioning can create a member document from the client. Added explicit unconditional deny matches for `householdInviteTokens`, `householdInviteManagement`, issue/redemption/revocation receipts, and `inviteRateLimitBuckets`; Firebase Admin SDK callable work continues to bypass Firestore Rules. |
| Adversarial evidence | Added a dual-profile emulator suite that first reads a public recipe containing `householdId`, derives the historical `KS-HOUSEH` code, and verifies the legacy invite `get`, collection `list`, and membership/user/household-count batch all fail. It also verifies ordinary authenticated users and household Admins cannot `get`, `list`, create, update, or delete documents in every server-only invite root. The suite preserves a successful reserved solo initial-provisioning batch and a normal existing-Admin household-name update. |
| Focused verification evidence | TDD red: `npm test -- invite-cutover-rules.test.ts` failed in both development and production profiles because the derived legacy invite `get` still succeeded. Green: `npm test -- invite-cutover-rules.test.ts firestore-rules.test.ts rule-profile-parity.test.ts` passed 3 files / 48 tests. The full `npm test` Rules emulator gate then passed 21 files / 342 tests. Profile parity confirmed `firestore.rules` and `firestore.dev.rules` are byte-identical. |
| Files | `firestore.rules`; `firestore.dev.rules`; `tools/rules_tests/invite-cutover-rules.test.ts`; `tools/rules_tests/firestore-rules.test.ts`; `tools/rules_tests/shopping-schedule-rules-security-scenarios.ts`; `tools/rules_tests/shopping-schedule-rules-test-helpers.ts`; this progress record. |
| Coordinated release ordering | Do **not** deploy this Rules change while released Flutter clients still use the direct `KS-*` flow: it intentionally breaks their invite join path. First provision and deploy the trusted callable backend/secrets, then ship and verify a client route that uses callable issuance/redemption/revocation behind a remotely controllable capability/feature switch. Disable direct legacy invite UI/requests for unsupported clients (or require an update), enable the callable route, and only then deploy the deny-only Rules cutover. Keep the server-side switch available for rollback of the new client route; never restore legacy Rules as the rollback mechanism. Legacy record cleanup can follow after the migration window. |
| Blockers | Flutter still calls the legacy client invite path; no production Secret Manager provisioning or Functions/Rules deployment has occurred; no terminal cleanup worker or legacy-record cleanup/migration has been delivered. A deployed Rules cutover before client migration is an intentional service break for legacy joins. |
| Next dependency-safe work | Migrate the client invite experience to the trusted callable contract with a controlled rollout/feature switch and add end-to-end coverage. Keep Rules deny-only throughout the rollout; do not reintroduce client invite document access or direct member creation. |

## Iteration 7 — Flutter trusted invite callable migration

| Field | Status |
| --- | --- |
| Dependency | Builds on the trusted issue/redeem callable contract and the checked-in deny-only legacy Rules cutover. This is a client trust-boundary migration only; it does not deploy Functions/Rules, provision secrets, or change production data. |
| Status | **Complete for this bounded client migration unit.** The compiled Flutter invite path no longer derives, reads, writes, displays from persisted state, or redeems `KS-*` Firestore invite records. P0 remains unreleased pending coordinated deployment, production secret provisioning, cleanup/migration work, and end-to-end verification. |
| Callable boundary | Added a narrow `HouseholdInviteRemoteDataSource` that invokes only `issueHouseholdInvite` and `redeemHouseholdInvite`. It strictly parses allowlisted response maps for request ID, household ID, allowed role, opaque invite ID, replay flag, and the fresh-only opaque token. Malformed/missing/unexpected response data fails with a generic client-safe error. No data source API reads/writes Firestore invite records or persists a token. |
| Issuance flow | New joint-household creation writes only the household and initial Admin membership; it no longer derives an ID-based code, writes `householdInvites`, or stores `inviteCode` on the household. The existing household-members area now exposes the issuance action only for a current joint-household Admin UI. It submits a generated command ID, household ID, and the fixed allowed `member` role. A raw token is displayed by the existing invite-code component only after a fresh response and exists only in that mounted widget state. A token-free exact replay never invents/retrieves a token and prompts the Admin to create a new invite. |
| Redemption flow | The onboarding field removes whitespace for paste ergonomics without changing token case. It locally gives the existing generic unavailable-invite message to obvious `KS-*` input, then sends only opaque token plus a generated command ID to `redeemHouseholdInvite`. Client direct invite lookup, member creation, capacity increment, profile/context mutation, and legacy fallback have been removed. On callable success it invalidates the household/session stream and picker so routing observes the backend-written context. Error text is allowlisted and never interpolates the token. |
| Rollout configuration | Repository inspection found no existing Remote Config or other controlled feature-switch pattern appropriate for this route. No new remote-config architecture was invented. The trusted callable route is therefore the only compiled route. Coordinate release by provisioning both Functions secrets and deploying callable backend first; ship/verify this client route; then deploy the already-prepared deny-only Rules cutover. Do not restore legacy Rules for rollback. Unsupported prior clients must be updated/blocked before Rules deployment. |
| Explicit non-goal | No revoke-management UI was added; the trusted `revokeHouseholdInvite` backend command remains available for a later bounded household-management unit. |
| Focused verification evidence | TDD red: `flutter test test/features/household/data/datasources/household_invite_remote_data_source_test.dart` initially failed because the callable data source did not exist. Green: focused Dart data-source and onboarding/household widget tests passed 3 files / 27 tests. They cover exact issue/redeem callable payloads, strict token/replay parsing, token-free replay prompt, transient raw-token display only after fresh issue, user-safe issue/join errors, legacy local rejection, case-preserving normalization, and Admin-only issuance UI. Focused Flutter analysis completed with no errors or warnings. |
| Files | `lib/features/household/data/datasources/household_invite_remote_data_source.dart`; `lib/features/household/presentation/controllers/household_invite_command_controller.dart`; `lib/features/onboarding/presentation/screens/household_setup_screen.dart`; `lib/features/household/presentation/screens/household_screen.dart`; `test/features/household/data/datasources/household_invite_remote_data_source_test.dart`; `test/features/onboarding/onboarding_screens_test.dart`; `test/features/household/household_screen_test.dart`; this progress record. |
| Blockers | Production Secret Manager provisioning and Functions deployment; coordinated client/Rules deployment; emulator-backed client-to-callable coverage; terminal cleanup worker; and legacy-record cleanup/migration remain. No remote switch exists for this route, so rollback must be a compatible callable/client release and must never restore legacy invite Rules. |
| Next dependency-safe work | Implement and test a bounded trusted terminal-invite cleanup worker that processes only opaque invite records, management indexes, and command receipts after their recorded retention eligibility. It must preserve active records, leave legacy records to a separately approved migration/disposition plan, use bounded idempotent batches, and never handle raw tokens. |

## Iteration 8 — bounded opaque invite terminal-metadata cleanup

| Field | Status |
| --- | --- |
| Dependency | Builds on opaque invite issuance/redemption/revocation/rate-limit records and the client/Rules trust-boundary cutover. This unit adds only a backend scheduled cleanup worker and associated server metadata consistency; it does not deploy, process production data, change Rules, change Flutter, or choose a legacy-record disposition. |
| Status | **Complete for the bounded cleanup-worker unit.** A scheduled Functions entrypoint now invokes a finite, allowlisted cleanup operation for eligible opaque invite metadata. P0 remains unreleased pending production secret provisioning/deployment, coordinated release execution, legacy-record disposition, and broader P0–P3 work. |
| Bounded worker | `cleanupTerminalInviteMetadataDaily` is an `onSchedule` backend-only Function running every 24 hours. Each invocation uses trusted runtime time, at most 100 candidate documents, per-collection pages of at most 25 documents, and delete batches of at most 100 writes (well below Firestore's 500-write limit). Queries are bounded, ordered only by the queried eligibility field, and use the existing automatic single-field indexes; no new composite index is required. A retry re-reads the same bounded eligible prefix and treats already-deleted records as absent, so reruns are idempotent. |
| Strict deletion scope | The worker's explicit allowlist is `householdInviteTokens`, `householdInviteManagement`, issue/redemption/revocation receipts, and `inviteRateLimitBuckets` only. It never queries, reads, writes, or deletes `householdInvites`; all legacy `KS-*` disposition remains deferred. The structured worker summary and scheduled log contain counts only—never household/user identifiers, token values, or token/rate HMACs. |
| Retention validation | Primary invites must have a valid opaque schema and a trusted eligibility timestamp at or before execution. It deletes only `redeemed` or `revoked` records whose 90-day eligibility exactly derives from their terminal event, or `active` records that are expired and whose eligibility exactly derives from stored expiry plus 90 days. Active unexpired and malformed/corrupt records are preserved. Receipt and rate records must have a valid collection-specific schema and elapsed cleanup timestamp; rate buckets additionally validate operation/scope/limit/count, window order, and the exact 30-day window-end retention derivation. |
| Partner consistency | Valid primary invite and management-index pairs are deleted in the same bounded batch only when IDs, household, HMAC lookup representation, terminal status, and eligibility match. A missing management partner permits deletion of an otherwise valid primary record; a present malformed/mismatched partner blocks deletion fail-closed. Management indexes with a missing primary are retained rather than guessed about. Future redemption now advances a present, matching management index to `redeemed` with the same terminal eligibility as its primary; pre-management opaque records still redeem normally. |
| IAM and Rules boundary | The scheduled Function runs as its deployment-selected Functions service account. Firebase Admin SDK access bypasses Firestore Security Rules and service-account IAM is database/project scoped, **not collection scoped**. The worker's hard-coded application collection allowlist plus an intentionally reviewed deployment identity are therefore its relevant scope boundaries; deployment must grant only the required database access and never rely on fictitious collection-level IAM. |
| Focused verification evidence | TDD red: `npx vitest run test/unit/inviteTerminalCleanup.test.ts` first failed because `inviteTerminalCleanup.js` did not exist. Green: focused invite/callable suites passed 7 files / 49 tests. The new cleanup suite covers exact eligibility, active preservation, malformed fail-closed records, expired-active cleanup, paired/missing-partner behavior, receipt/rate cleanup, batch and candidate limits, retry idempotency, and explicit legacy exclusion. `npm run build` passed. Focused Biome check across all changed Functions sources/tests passed. A full `npm run lint` was also run; it remains blocked solely by the pre-existing unrelated `src/callableSecurity.ts` literal-key lint finding. |
| Files | `functions/src/invites/inviteTerminalCleanup.ts`; `functions/src/invites/inviteRateLimit.ts`; `functions/src/invites/inviteRedemption.ts`; `functions/src/index.ts`; `functions/test/unit/inviteTerminalCleanup.test.ts`; this progress record. |
| Blockers | Production Secret Manager provisioning and Functions deployment; coordinated callable/client/Rules production release; emulator-backed deployed-Functions integration; deliberate legacy `householdInvites` disposition/migration; and the full admin P0–P3 implementation remain. The worker intentionally leaves malformed or orphaned management metadata in place for audited remediation rather than deleting uncertain data. |
| Next dependency-safe work | Add a narrow deployed-Functions/emulator integration check for the scheduled cleanup entrypoint and opaque records, then prepare an operator-reviewed production deployment checklist for secrets, service-account identity, callable rollout, and deny-only Rules sequencing. Do not add legacy cleanup without an explicitly approved disposition plan. |

## Iteration 10 — Firestore-emulator invite handler integration

| Field | Status |
| --- | --- |
| Dependency | Retries the failed Iteration 9 scope as one narrower unit: direct trusted invite handlers against the real Firestore Emulator. It intentionally does not combine callable HTTP/App Check transport, Firebase Auth transport, Cloud Scheduler/Pub/Sub delivery, or duplicate Firestore Rules tests. |
| Status | **Complete for the bounded handler-level emulator unit.** Opaque issue, redemption, final-capacity contention, legacy-format rejection, and direct cleanup behavior now execute against real Firestore transactions. P0 remains unreleased pending production setup/release execution, remaining transport coverage, legacy disposition, and P0–P3 delivery. |
| Handler integration evidence | A new Functions emulator test creates isolated Admin-SDK emulator apps with deterministic test-only HMAC/rate-limit dependencies. It seeds a trusted joint Premium household/Admin, invokes `issueHouseholdInviteHandler`, and confirms only opaque/HMAC metadata, management state, and receipts persist; the raw invite token exists only on a fresh in-process handler response and is absent from the replay/persisted field allowlist. Assertions deliberately never interpolate or print a raw token. |
| Redemption and contention evidence | The suite invokes `redeemHouseholdInviteHandler` in real Firestore transactions and verifies member creation, profile context, household count, primary/management consumption, and exact idempotent replay. A `KS-*` redemption is generically rejected without member/profile creation or count change. Two distinct opaque invites race for the final capacity slot; exactly one handler succeeds and exactly one membership is committed. |
| Cleanup evidence | The suite creates real terminal opaque metadata through issue/redeem, advances trusted time past retention, invokes `cleanupTerminalInviteMetadata` directly, and verifies primary/index deletion. It also seeds `householdInvites/KS-*` and proves the legacy document remains untouched. This validates cleanup logic, not scheduled-trigger delivery. |
| Separate Rules evidence | Client/Roles denial is deliberately not duplicated here. Iteration 6's existing dual-profile Rules emulator gate remains the evidence for unreadable legacy `KS-*` records, denied client membership batches, and denied server-only invite collection access. |
| TDD / verification evidence | Red: the exact Firebase emulator runner initially reported no focused test file; the first new cleanup assertion also exposed that same-emulator earlier fixtures were valid cleanup candidates. The fixture timestamp was isolated instead of weakening the exact deletion assertion. Green: `tools/firebase-gates/firebase.sh --config firebase.dev.json emulators:exec --only auth,firestore,functions,storage --project kitchensync-dev-da503 "npm --prefix functions run test:emulator -- inviteHandlerIntegration.test.ts"` passed 1 file / 5 tests. Focused invite unit suites passed 7 files / 49 tests; `npm run build`, changed-file Biome, and `git diff --check` passed. |
| Explicit limitations | Full callable HTTP transport, App Check enforcement/claims behavior, Firebase Auth transport identity propagation, and scheduled Cloud Scheduler/Pub/Sub trigger transport are **not** verified by this unit. The test invokes handlers/cleanup directly after the existing runner launches the emulator environment. No production service account, secret, deployment, data, Rule deployment, or client release was changed. |
| Operator-reviewed release checklist | Before release, an operator must: 1. provision `INVITE_TOKEN_HMAC_KEY` and `INVITE_RATE_LIMIT_KEY` in Secret Manager; 2. review the Functions deployment service account and its database-scoped IAM (Admin SDK bypasses Firestore Rules; IAM is not collection scoped); 3. deploy Functions; 4. run staging and emulator gates; 5. require an updated callable-based client; 6. deploy the already-reviewed deny-only Rules; 7. monitor request IDs plus generic error and rate-limit metrics; and 8. roll back only with a compatible callable/client release—**never** by restoring legacy invite Rules. |
| Files | `functions/test/emulator/inviteHandlerIntegration.test.ts`; this progress record. |
| Blockers | Operator execution of the release checklist; callable/App Check and scheduler-trigger transport coverage; approved legacy `householdInvites` disposition; production monitoring; and the remaining P0–P3 work. |
| Next dependency-safe work | Escalate release execution to an operator/security owner: validate transport controls in staging, provision/deploy according to the checklist, and decide legacy data disposition. Do not expand this test unit into a transport test without dedicated emulator/App Check scope. |

## Iteration 11 — consolidated admin read-only slice and release hygiene

| Field | Status |
| --- | --- |
| Dependency | Consolidates the completed repository work for opaque invite controls/cleanup, deny-only Rules boundaries, the admin P0/P1 read-only backend and SPA, Hosting target, CI gates, operator documentation, and the approved staff-identity/consumer-revocation ADR. It is not a production deployment or a mutation-capability release. |
| Status | **Repository implementation and the two policy decisions are complete for the bounded P0/P1 read-only slice; deployment/release remains incomplete.** No claim is made for P2/P3 workflows or full architecture completion. |
| Admin read-only slice | `adminHealthGet`, `adminUserGet`, `adminHouseholdGet`, and `adminEntitlementGet` are statically registered, strict-schema callable reads. Human authorization fails closed on non-anonymous Auth, expected project/audience, verified App Check admin app ID, provider/tenant/MFA evidence, recent `auth_time`, then a candidate audit actor before `platformStaff: true`, authoritative staff, capability, or scope decisions. Health has a five-minute freshness bound. Every current operation, including health, re-verifies the raw token with `verifyIdToken(rawToken, true)`. Post-framework claim/staff/capability/scope denials receive best-effort HMAC-only denial audits; successful sensitive reads synchronously persist audits and fail closed if that persistence fails. No generic search, field-mask selection, patch, import, privacy, moderation, billing, repair, or mutation command is released. |
| Staff/runtime/CORS control | The human `platform_staff/{uid}` shape is `enabled: true`, `staffType: employee | contractor`, bounded documented roles/capabilities, bounded `scope.environments`, `mfaRequired: true`, and policy version. `enabled: true` cannot coexist with `disabledAt`; an optional region scope must include deployed `us-central1`; queues are non-authorizing; and the recognized `break_glass` role is denied for every current endpoint. Service identities are excluded. Required non-secret runtime configuration includes exact `ADMIN_ALLOWED_ORIGINS`, `ADMIN_RATE_LIMIT_KEY_VERSION`, and `ADMIN_AUDIT_HMAC_KEY_VERSION`; callable CORS defaults to `false` when configuration is missing/invalid and is never an authorization substitute. |
| Approved identity, offboarding, and consumer revocation policy | [`admin_staff_identity_and_consumer_revocation_adr.md`](admin_staff_identity_and_consumer_revocation_adr.md) approves dedicated same-project, non-tenant staff UIDs only: email/password plus phone SMS MFA, no consumer/staff dual use, `platformStaff: true` plus authoritative least-privilege `platform_staff/{uid}`. Development may use configured test phone numbers; production requires a separately controlled real staff identity and enrolled real phone factor. Offboarding order is staff-record disable, claim removal, refresh-token revocation, then optional Auth-account disable. Consumer direct Firestore/Storage revocation is eventual, with up to 60 minutes of existing ID-token lifetime; immediate revocation is not claimed. Admin callables continue `checkRevoked=true`, and console focus/periodic revalidation only reduce residual displayed UI. |
| Key separation and audit assurance | `ADMIN_RATE_LIMIT_KEY` and `ADMIN_AUDIT_HMAC_KEY` are separate Secret Manager-only canonical-base64url HMAC secrets, each requiring at least 32 CSPRNG bytes and never belonging in an environment file. The rate key produces only rate-bucket identities; the audit key produces only audit actor/case/target/app and household-member references. Versioned HMAC values and persisted key-version fields support audited key rotation. Audit records also contain only non-sensitive assurance metadata: environment, roles/capabilities used, required capability, provider, tenant classification, second-factor classification, auth age, and an HMAC app reference. |
| DTO and entitlement contract | DTOs remain fixed/redacted. User 360 masks identity and returns `contextConsistency: "missing"` plus `entitlement: null` when no profile or household context exists. Household member summaries use HMAC `memberRef` values; nested household metadata no longer duplicates counts, and top-level `capacity` is canonical. Entitlement accepts only `household.menu_sets`. Its `productionAccess` independently mirrors the current Rules predicate: `hasPremium === true` plus an absent, null, or future trusted timestamp `premiumTrialEndsAt`. Separately, `billingConsistency` reports observed subscription/owner-profile evidence without changing that Rules-equivalent production-access result. |
| Invite cleanup fairness/cursor hardening | The scheduled opaque invite terminal-metadata worker now rotates fair per-collection candidate budgets and persists server-only cursors in `serverInviteTerminalCleanupCursors`. Ordered cursor paging wraps after the newest eligible record, preventing one busy collection/page prefix from indefinitely starving others. Cursor advances follow committed work; concurrent movement can repeat work but does not skip a page. The worker remains bounded, allowlisted, idempotent, and explicitly excludes legacy `householdInvites/KS-*` records. |
| Rules boundary | Both Rules profiles deny client access to admin-control roots and server-only invite/cleanup roots while preserving ordinary permitted household operations. Admin SDK Functions access is still privileged infrastructure and is constrained by application authorization/allowlists, not imagined collection-level IAM. The repository boundary is not proof that Rules were deployed to any Firebase project. |
| Mutation posture | All P2/P3 customer-state mutation classes remain disabled. The approved identity/revocation ADR does not authorize privacy, legal-hold, moderation, billing, account-control, or other customer-state mutation work. |
| SPA, Hosting, and CI | The separate `apps/admin-web` React/Vite SPA has typed callable decoding, masked views, session boundary, environment display, and a classic Hosting target named `admin`. `VITE_APP_CHECK_SITE_KEY` is a required public build input. The SPA initializes reCAPTCHA Enterprise App Check before Functions, enables token auto-refresh, implements the phone MFA resolver/SMS-code flow, and has explicit CSP origin allowlists for Firebase, Functions, Google, and reCAPTCHA dependencies. CI/repository gates cover Functions, the SPA, Rules, Firebase verification, and rollout-contract behavior. Development uses `firebase.dev.json` (with root `firebase.json` as the default local/development configuration); production deployment must explicitly use `--config firebase.prod.json`. |
| Final verified repository evidence | Functions unit gate: **17 files / 153 tests**. Admin-handler Firestore Emulator gate: **1 file / 4 tests**. Admin-web unit gate: **11 files / 31 tests**. Playwright: **4 tests** across desktop and mobile. Focused Rules: **3 files / 11 tests**. Firebase verifier and rollout-contract gates pass; the verifier validates `firebase.json`, `firebase.dev.json`, and `firebase.prod.json`, including environment-exact CSP. Functions build passes; Functions lint passes with informational `useLiteralKeys` suggestions only. None of this is staging or deployed transport evidence. |
| Historical configuration exposure evidence | `functions/.env.kitchensync-dev-da503` was historically tracked in commit `f471a21`; its parent removed it from Git tracking with `git rm --cached` while preserving the local ignored file. It has not been history-purged, and this iteration does not assert that its values were rotated. Its contents were not recovered or printed. Every credential/secret-like value ever present must be treated as exposed; public Firebase identifiers still require environment/configuration review. |
| Repository versus deployment | At the Iteration 11 repository snapshot, no evidence established a real registered App Check Enterprise token exchange, real phone MFA/SMS delivery, deployed CORS/Hosting headers, Secret Manager values or IAM, real revocation behavior, human staff claims/records, callable transport, audit/rate writes, or Functions/Hosting deployment. Iteration 12 subsequently records development-only operator evidence for those applicable development controls; it does not establish a production deployment or production data change. |
| Explicit blockers | Production remains blocked on real separately controlled staff-identity enrollment and phone MFA/SMS delivery; Secret Manager/IAM provisioning and key-rotation/history retention; target-environment App Check, CORS/Hosting-header, callable transport, and real-revocation evidence; confirmation of the approved eventual consumer direct Firestore/Storage revocation behavior; security-owner environment-file inventory, rotation/revocation, and history response; opaque-invite release execution, client/version sequencing, deny-only Rules preservation, and approved legacy-invite disposition; and Functions/Hosting deployment. Repository-history purge is considered only through an explicitly approved coordinated process. |
| Next dependency-safe work | Provision a non-production environment from the runbook; enroll a real separately controlled staff identity and phone factor; validate the external transport and approved eventual-revocation behavior without claiming it from repository tests; and record the resulting operator evidence. Keep every mutation class off and never restore legacy invite Rules during rollback. |

## Iteration 12 — recorded development deployment evidence

| Field | Status |
| --- | --- |
| Date and status | **2026-08-01 — development evidence recorded; production remains unapproved and blocked.** This iteration records operator-observed evidence in `kitchensync-dev-da503`; it does not expand the released feature scope. |
| Development deployment | The separate admin Web App, Hosting site/release, registered reCAPTCHA Enterprise App Check configuration, Identity Platform optional phone-SMS MFA configuration, version-1 server secrets, custom-role runtime identities, eight active Node.js 22 Functions revisions, 24-hour `Etc/UTC` cleanup schedule, Firestore/Storage Rules, and Hosting configuration were deployed in development. Exact non-sensitive identifiers are in [`admin_dashboard_development_release_record.md`](admin_dashboard_development_release_record.md). |
| Live evidence | A controlled headed browser observed the hosted CSP, password plus fictional test-phone MFA, real App Check, successful health and masked User 360 paths, invalid-App-Check `401 UNAUTHENTICATED`, invalid-origin browser mismatch, and revoked-token `403 PERMISSION_DENIED` denial across all four admin operations. Development audit inspection confirmed the fixed HMAC-only reference shape, and rate-bucket inspection confirmed redacted metadata only. A separate disposable ordinary-user canary on the hosted admin Web App obtained real App Check; deployed invite issue/redeem/revoke transport succeeded; persisted membership/capacity and terminal status transitions were correct; raw bearer tokens were absent from persisted invite documents; and new rate metadata had no raw user/household/token values. The two canary Auth users, test data, rate buckets, and credential/App Check files were deleted without recording sensitive details. Development invite callable transport is now evidenced. |
| Local evidence | Clean-snapshot gates recorded: Functions 18 files / 161 tests; admin web 11 files / 31 tests; Playwright 4 / 4; Rules 22 files / 344 tests; emulator handlers 2 files / 9 tests; Flutter invite 3 files / 27 tests; Firebase verifier 7 groups; rollout contracts 11. The Functions production dependency audit has zero high/critical findings after lockfile-only remediation; nine moderate transitive advisories remain, and the suggested breaking Firebase Admin change was rejected. |
| Deployment hardening learned | The non-incremental deployment emit avoids stale build metadata; full-email `defineString` service-account parameters replace shorthand principals; and controlled headed-browser App Check testing is retained because headless reCAPTCHA stalled. |
| Remaining blockers | Production billing/resource provisioning, deployment, and approvals; a real production human staff identity and real phone factor; production App Check/transport/revocation canary; updated mobile invite-client distribution/enforcement and approved legacy-invite inventory/disposition; historical tracked environment-file inventory/rotation/history decision; an observation/monitoring period; and every P2/P3 mutation policy/workflow. All P2/P3 classes remain disabled and out of scope. |
