{
  "name": "kitchensync-functions",
  "private": true,
  "type": "module",
  "engines": {
    "node": "22"
  },
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc --noEmit && tsc -p tsconfig.build.json",
    "lint": "biome check .",
    "test": "vitest run test/unit",
    "test:emulator": "vitest run --pool=forks --testTimeout=30000"
  },
  "dependencies": {
    "firebase-admin": "^13.10.0",
    "firebase-functions": "^7.2.5",
    "google-auth-library": "^10.9.0",
    "undici": "^7.24.3",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.3",
    "@types/node": "^22.10.2",
    "firebase": "^12.8.0",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
